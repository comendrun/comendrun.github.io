import { IconType } from "react-icons";

export type ExpCardTypes = {
  title: string;
  company: string;
  companyWebsite: string;
  duration: string;
  description: string | JSX.Element;
  logo: string
};
